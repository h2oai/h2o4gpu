numpydoc==1.6.0
sphinx==5.3.0
sphinx_rtd_theme==0.5.2
Jinja2<3.1
pillow==9.5.0
wheel==0.45.1
# >=3.0.10 required to build scikit-learn 1.5.2 from source. Intentionally NOT
# synced to DAI's Cython 0.29.37 (that is DAI build-only and cannot build sklearn 1.5).
cython==3.0.11
pandas==1.5.3
numpy==1.24.4
scipy==1.12.0
numba==0.59.1
psutil==5.9.8
llvmlite==0.42.0
future==1.0.0
tabulate==0.10.0
joblib>=1.1.1
# the same version as scikit-learn fork; kept in sync with Driverless AI (DAI)
scikit-learn==1.5.2
# NOTE: dask / distributed / dask-cuda removed - only used by the (DAI-unused)
# distributed multi-GPU XGBoost path in solvers/xgboost.py, which lazy-imports
# them behind guards. DAI is dropping dask; the old pins would only conflict.
msgpack==1.1.2